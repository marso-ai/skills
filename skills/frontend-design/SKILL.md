---
name: frontend-design
description: Use when performing any frontend or UI task. Ensures design guidelines are followed before making changes.
allowed-tools: Read, Write, Edit, Glob
---

Before doing anything else, read the design rules file at `.claude/rules/m-xr-design.md` and keep them in mind for the entire task.

If the file does not exist, warn the user and suggest they run `/ui-lib` first to set it up.

Then proceed with the user's requested frontend task: $ARGUMENTS